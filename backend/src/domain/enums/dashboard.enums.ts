export enum HomeTargetKind {
  Note = 'note',
  Project = 'project',
}

export enum HomePriorityType {
  Reminder = 'reminder',
  Finding = 'finding',
  Incident = 'incident',
  Followup = 'followup',
}
