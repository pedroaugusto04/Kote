export enum AiProvider {
  OpenRouter = 'openrouter',
  OpenAi = 'openai',
  Gemini = 'gemini',
  None = 'none',
}

export enum ConversationConfidence {
  High = 'high',
  Medium = 'medium',
  Low = 'low',
}
